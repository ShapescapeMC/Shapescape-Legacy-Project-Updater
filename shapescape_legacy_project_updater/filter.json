{
	"description": "A filter for updating project using legacy commands - old execute format, block commands (fill, setblock, execute .. if block), and entity commands (summon).",
	"filters": [
		{
			"runWith": "python",
			"script": "./main.py",
			"name": "shapescape:shapescape_legacy_project_updater"
		}
	]
}
